## Worktrees

When asked to do something in your own worktree, create it under .worktrees/ with prefix auto-<branch>

## Workspaces

- `apps/client`: browser game client built with Vite.
- `apps/server`: server-side application code.
- `packages/common`: shared code used by client and server.
